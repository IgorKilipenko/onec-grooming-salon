
Функция ШаблонURL_GET_userGetUser(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	
	результат = ЗаписатьЗначениеJSON( 
		Новый Структура("Пользователь", Новый Структура("Имя", Запрос.ПараметрыЗапроса.Получить("name"))));
	
	Ответ.УстановитьТелоИзСтроки(результат);
	Ответ.Заголовки.Вставить("Content-type", "application/json");

	Возврат Ответ;
КонецФункции

Функция ШаблонURL_UsersCreateUser(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
	Возврат Ответ;
КонецФункции
